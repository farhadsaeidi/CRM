# CRM — سامانهٔ مدیریت مشتریان

بازنویسی پروژهٔ `CustomerManagement` روی معماری **DRF + React**، هم‌سبک با پروژهٔ SAM.
دفترِ حسابِ مشتریان یک کسب‌وکار است: هر مالک، مشتریانش را دارد و برای هر مشتری
تراکنش‌های نسیه/پرداختی ثبت می‌کند و وضعیت حساب (بدهکار/بستانکار/بی‌حساب) محاسبه می‌شود.
رابط کاربری کاملاً **فارسی و RTL** است.

**وضعیت فعلی: کارآمد و تست‌شده.** احراز هویت، مشتریان، تراکنش‌ها و داشبوردِ
تحلیلی همه کار می‌کنند و ۱۵۴ تستِ خودکار پشتشان است. آنچه مانده قابلیت‌های تازه
(گام ۱۰ و ۱۱) و استقرار (گام ۱۲) است.

```bash
venv/bin/python manage.py test        # ۱۵۴ تست، روی دیتابیسِ تستِ جداگانه
```

راهنمای معماری در `.claude/architecture.md` و نقشهٔ کد در `.claude/code-map.md`
(خودکار، با `bash scripts/gen_code_map.sh`). راه‌اندازی در `README.md`.

دانشِ انباشتهٔ جلسه‌های قبل در `.claude/memory/` است و `MEMORY.md` فهرستِ آن‌هاست.
اگر چیزی در آن‌ها با کدِ امروز نخواند، کد مرجع است.

## محیط

توسعه داخل **WSL Ubuntu** انجام می‌شود:

```bash
cd /home/farhad/projects/python/django/CRM
venv/bin/python manage.py runserver 8000     # ./manage.py کار نمی‌کند (شبنگ python است، اوبونتو فقط python3 دارد)
cd frontend && npm run dev                   # :5173، پراکسی /api و /media و /static و /django-admin به :8000
```

محیط مجازی اینجا `venv/` است، نه `.venv/` (برخلاف SAM). هر دو در `.gitignore` هستند.

> 📌 **صاحبِ پروژه با `http://localhost:5173` کار می‌کند و برایش باز می‌شود.**
> پس این آدرس را در گفتگو «غلط» نگویید.
>
> ✅ **از ۲۰۲۶-۰۸-۲۷ باز کردن با آی‌پیِ شبکه هم کار می‌کند.** پیش‌تر
> `CSRF_TRUSTED_ORIGINS` فقط `localhost` و `127.0.0.1` را می‌شناخت و هر
> POST/PATCH/DELETE از `http://192.168.1.22:5173` با ۴۰۳ و پیامِ
> «Origin checking failed» رد می‌شد. حالا `_local_ipv4s()` در `settings.py`
> آی‌پیِ محلی را **خودکار** پیدا می‌کند و به `CSRF_TRUSTED_ORIGINS` و
> `ALLOWED_HOSTS` اضافه می‌کند — خودکار و نه دستی، چون آی‌پی با DHCP عوض
> می‌شود و آدرسِ ثابت در کد یعنی برگشتِ همین خطا چند وقت بعد.
>
> ⚠️ تشخیص با **سوکتِ UDP** انجام می‌شود نه `gethostbyname_ex`؛ دومی داخلِ WSL
> فقط `127.0.1.1` می‌دهد (آزموده شد). برای آدرسِ غیرمعمول (مثلاً یک هاست‌نیمِ
> محلی) `DEV_TRUSTED_ORIGINS` در `.env` هست و کد دست نمی‌خورد.
>
> ⚠️ **ولی از سمتِ ابزارها `localhost:5173` به سرورِ WSL نمی‌رسد** (تست شد:
> connection refused در حالی که سرور بالا بود). رلهٔ localhostِ ویندوز↔WSL اینجا
> کار نمی‌کند — شبکه در حالت NAT است و یک پراکسیِ localhost روی ویندوز فعال که
> WSL نمی‌تواند آینه‌اش کند (خودش هم هشدار می‌دهد). برای تستِ خودکار با IPِ WSL
> باز کنید:
>
> ```bash
> wsl -e bash -lc "hostname -I | awk '{print \$1}'"   # مثلاً 192.168.1.22
> ```
>
> `server.host: true` در `vite.config.js` هست تا Vite روی همهٔ اینترفیس‌ها بایند شود؛
> بدون آن حتی با IP هم در دسترس نیست. اگر روزی خواستید `localhost` را برگردانید،
> باید `autoProxy=false` در `~/.wslconfig` ست شود و WSL ری‌استارت گردد.

دستورها را با `wsl -e env -i HOME=/home/farhad /bin/bash -lc '...'` اجرا کنید؛ `wsl` خالی
متغیرهای ویندوز را به ارث می‌برد و `$HOME` را خراب می‌کند. **اگر از Git Bash ویندوز صدا
می‌زنید**، حتماً `MSYS_NO_PATHCONV=1` را جلویش بگذارید وگرنه `/bin/bash` به مسیر ویندوزی
ترجمه می‌شود و خطای `env: 'C:/Program Files/Git/usr/bin/bash': No such file or directory`
می‌گیرید.

> ⚠️ **`HOME=/home/farhad` را جا نیندازید.** خودِ `env -i` هم `HOME` را پاک می‌کند و
> شلِ لاگین برش نمی‌گرداند. بدون آن گیت نه `~/.gitconfig` را می‌خواند نه
> `credential.helper` را، سراغ پرسیدن یوزر/پسورد می‌رود و چون ورودی تعاملی نیست
> **تا ابد معلق می‌ماند** — نه خطا می‌دهد نه timeout.

نسخهٔ ویندوزی در `D:\Python\Django\CRM` مبدأ همین کپی است و همان یک کامیت را دارد؛
از این به بعد فقط نسخهٔ WSL را ویرایش کنید تا دو شاخه نشوند.

پروژهٔ اصلیِ قدیمی (مرجعِ منطق کسب‌وکار) در `D:\Python\Django\CustomerManagement` است.

## دیتابیس — تلهٔ پورت

دیتابیس روی **پستگرس ۱۸ لینوکس، پورت ۵۴۳۴** است — دقیقاً مثل SAM. داده‌ها روز
۱۴۰۵/۰۵/۲۹ (۲۰۲۶-۰۸-۲۰) با `pg_dump` از پستگرس ویندوز منتقل شدند.
جدول‌ها: `account_owner`، `home_customer`، `home_customerowner`، `home_transaction`.

چهار نمونهٔ پستگرس روی این ماشین در دسترس‌اند:

| پورت | نمونه | دیتابیس‌ها |
|---|---|---|
| ۵۴۳۲ | PostgreSQL 17 ویندوز | دیتابیس‌های نامرتبط — **`CRM` ندارد** |
| ۵۴۳۳ | PostgreSQL 18 ویندوز | `CRM` **کهنه** (نسخهٔ پیش از انتقال) و `SAM` کهنه |
| **۵۴۳۴** | **PostgreSQL 18 لینوکس** | **`CRM` زنده — همانی که پروژه استفاده می‌کند** و `SAM` |

> ⚠️ **دو `CRM` وجود دارد.** آنکه روی ۵۴۳۳ است دیگر مرجع نیست و از لحظهٔ انتقال
> فریز شده؛ همان است که پروژهٔ قدیمی `CustomerManagement` هنوز استفاده می‌کند. اگر
> اشتباهی به آن وصل شوید هیچ خطایی نمی‌گیرید — فقط روی دادهٔ کهنه کار می‌کنید و
> تغییراتتان جای دیگری می‌رود. همیشه پورت را صریح بنویسید.

کلاستر لینوکس از ۲۰۲۶-۰۸-۲۰ **خودکار** بالا می‌آید: این توزیع systemd ندارد، پس در
`/etc/wsl.conf` یک قلاب گذاشته شده که هنگام boot به‌صورت root اجرا می‌شود:

```ini
[boot]
command = /usr/bin/pg_ctlcluster 18 main start
```

اگر باز هم `pg_lsclusters` وضعیت `down` نشان داد، دستی بالا بیاورید. `sudo` اینجا رمز
می‌خواهد ولی `wsl -u root` از سمت ویندوز بدون رمز دسترسی root می‌دهد:

```bash
wsl -u root -e pg_ctlcluster 18 main start
```

اتصال دستی:

```bash
psql -h 127.0.0.1 -p 5434 -U postgres -d CRM
```

تنظیمات از `.env` با `python-decouple` خوانده می‌شود (`DB_NAME`، `DB_USER`، `DB_PASSWORD`،
`DB_HOST`، `DB_PORT`). فایل `.env` در gitignore است و مجوزش `600` — مقادیرش را در
خروجی چاپ نکنید.

**نکته دربارهٔ نسخهٔ ویندوزیِ پروژه.** `.env` آن هنوز به ۵۴۳۳ اشاره دارد و باید همان
بماند: از داخل WSL نمی‌شود به پستگرس ویندوز رسید (`.wslconfig` روی `networkingMode=NAT`
است و فایروال ویندوز اتصال از سابنتِ WSL را drop می‌کند) و برعکسش هم لازم نیست.
یعنی دو نسخهٔ پروژه دو دیتابیس جدا دارند؛ نسخهٔ WSL مرجع است.

## مایگریشن و جدول‌های `legacy_*`

تاریخچهٔ مایگریشن در ۲۰۲۶-۰۸-۲۰ **از نو شروع شد**. تاریخچهٔ پروژهٔ قدیمی به اپ‌ها و
مدل‌هایی ارجاع می‌داد که بازنویسی نمی‌شوند (`account.Owner`)، و چون
`AUTH_USER_MODEL` به `account.MyUser` تغییر کرد نگه‌داشتنش ممکن نبود — جنگو تغییر
مدل کاربر روی تاریخچهٔ موجود را پشتیبانی نمی‌کند.

آنچه انجام شد: جدول‌های دادهٔ قدیمی به `legacy_*` تغییر نام دادند (هیچ‌چیز حذف نشد)،
جدول‌های دفترداریِ جنگو دور ریخته شدند، `migrate` تازه اجرا شد، و داده با `INSERT
… SELECT` کپی شد.

| جدول legacy | رکورد | مقصدش |
|---|---|---|
| `legacy_account_owner` | ۱۲ | ✅ در گام ۱ به `account_myuser` کپی شد |
| `legacy_home_customer` | ۳۲ | ⬜ گام ۳ |
| `legacy_home_customerowner` | ۳۲ | ⬜ گام ۳ |
| `legacy_home_transaction` | ۴۳ | ⬜ گام ۳ |

> **شناسه‌ها را موقع کپی حفظ کنید.** جدول‌های `legacy_home_*` با همان idها به مالک
> ارجاع می‌دهند؛ اگر id عوض شود پیوندِ مشتری‌ها و تراکنش‌ها با مالکشان می‌شکند.
> بعد از هر `INSERT … SELECT` هم حتماً شمارندهٔ identity را با `setval` جلو ببرید،
> وگرنه اولین رکوردِ تازه با id=1 تصادم می‌کند.

جدول‌های `legacy_*` تا پایان گام ۳ نگه داشته می‌شوند. حذفشان با شماست.

پیش از هر تغییر ساختاری پشتیبان بگیرید:

```bash
pg_dump -h localhost -p 5434 -U postgres -d CRM --no-owner --no-privileges -f backup.sql
```

## مسیر توسعه — گام‌به‌گام

ترتیب عمدی است: هر لایه **قرارداد** لایهٔ بعد را می‌سازد (مدل → API → کلاینت).
فرانت هرگز نباید پیش از تثبیت شکل دادهٔ سرور جلو برود. همان توالی‌ای که در SAM جواب داد.

**وضعیت را همین‌جا به‌روز نگه دارید** — با تمام شدن هر گام، علامتش را عوض کنید و یک
خط توضیح زیرش بنویسید. جزئیاتِ هر گام در `.claude/architecture.md` می‌آید و
شرحِ مفصلِ وضعیت در `.claude/memory/crm-skeleton-state.md`.

| گام | عنوان | وضعیت |
|---|---|---|
| ۰ | پیکربندی و اسکلت | ✅ انجام شد |
| ۱ | اپ `account` و مدل `MyUser` | ✅ انجام شد |
| ۲ | لایهٔ احراز هویت | ✅ انجام شد |
| ۳ | اپ `home` و مدل‌های دامنه | ✅ انجام شد |
| ۴ | API مشتریان | ✅ انجام شد |
| ۵ | API تراکنش‌ها | ✅ انجام شد |
| ۶ | اسکلت احراز هویتِ فرانت | ✅ انجام شد |
| ۷ | صفحهٔ مشتریان | ✅ انجام شد |
| ۸ | صفحهٔ تراکنش‌ها | ✅ انجام شد |
| ۸.۵ | ساختارِ صفحه‌ها (داشبورد/مشتریان/تراکنش‌ها/گفتگو) | ✅ انجام شد |
| ۸.۷ | داشبورد و تحلیل | ✅ انجام شد |
| ۹ | پالایش و تست | ✅ انجام شد |
| ۱۰ | تکمیل «به‌زودی»ها (پروفایل، گزارش‌ها، یادآوری، ورود با گوگل) | 🔸 نیمه‌کاره |
| ۱۱ | دستیارِ گفتگو | ⬜ |
| ۱۲ | استقرار | ⬜ |

**گام ۰ — پیکربندی و اسکلت** ✅ (۲۰۲۶-۰۸-۲۰)
پروژهٔ جنگو + DRF (سشن و CSRF، پیش‌فرض `IsAuthenticated`)، اتصال پستگرس از `.env`،
اسکلت Vite + React ۱۹ + Tailwind v4 با پراکسی، تمِ کامل و RTL، `api/client.js`،
روتینگ و صفحهٔ ۴۰۴. تنها endpoint: `/api/health/`. انتقال به WSL و آوردن دیتابیس
به کلاستر لینوکس هم در همین گام انجام شد.

**گام ۱ — اپ `account` و مدل `MyUser`** ✅ (۲۰۲۶-۰۸-۲۰)
`MyUser(AbstractBaseUser)` با `phone` به‌عنوان `USERNAME_FIELD` و
`AUTH_USER_MODEL = "account.MyUser"`. نقش‌ها فقط `owner` و `superuser`؛ `is_superuser`
مثل SAM پراپرتیِ مشتق از نقش است نه ستون. تاریخچهٔ مایگریشن از نو شروع شد و ۱۲ کاربر
با حفظ id و هشِ رمز از `legacy_account_owner` کپی شدند — بخش «مایگریشن» بالا.

**گام ۲ — لایهٔ احراز هویت** ✅ (۲۰۲۶-۰۸-۲۰)
هشت endpoint روی الگوی `account/views.py` در SAM: `csrf` / `me` / `register` /
`login` / `logout` / `otp/phone` / `otp/confirm` / `forget-password` /
`change-password`. `UserSerializer` فقط‌خواندنی.

نکته‌هایی که موقع تست تثبیت شدند:
- ارقام فارسی/عربی و پیش‌شمارهٔ `+98`/`0098` هم در شماره و هم در کد تایید نرمال می‌شوند.
- کد یکبار مصرف ۵ رقمی، اعتبار ۱۲۰ ثانیه، حداکثر ۵ تلاش (شمارش سمت سرور در cache،
  نه کوکی، تا با پاک کردن کوکی دور نخورد). تلاش ششم → ۴۲۹ و کد باطل می‌شود.
- درخواست کد تا وقتی کد قبلی معتبر است ۴۲۹ می‌دهد با `remainingSeconds`.
- در «فراموشی رمز» اول پیامک ارسال می‌شود بعد رمز عوض — برعکسش یعنی اگر ارسال
  بشکند کاربر بیرونِ حساب خودش قفل می‌شود.
- `change-password` بعد از تغییر `update_session_auth_hash` می‌زند وگرنه کاربر
  بلافاصله از سشن خودش بیرون می‌افتد.

**پیامک.** سرویس در `account/services.py` است و تنها جایی است که با کاوه‌نگار حرف
می‌زند. `SMSLog` هم در همان اپ `account` است (نه یک اپ `comms` جدا مثل SAM، چون
CRM دامنهٔ پیام‌رسانی ندارد و تنها پیامک‌هایش احراز هویتی‌اند).

با `SMS_DEV_MODE=True` هیچ پیامکی ارسال نمی‌شود و فقط در `SMSLog` ثبت می‌گردد —
لازم است چون کاوه‌نگار فقط به شماره‌های تاییدشدهٔ صاحب حساب ارسال دارد و شماره‌های
تست همیشه ۵۰۲ می‌گیرند. برای تست فلوی تایید، کد را از دیتابیس بخوانید:

```bash
venv/bin/python manage.py shell -c "from account.models import MyUser; print(MyUser.objects.get(phone='09120000000').otp)"
```

> **هرگز کد یا رمز داخل `SMSLog` ذخیره نمی‌شود** — این جدول از پنل ادمین خواندنی
> است و لاگ نباید به کلیدِ ورود تبدیل شود. فقط نامِ الگو و رویداد ثبت می‌گردد.

سه الگو در پنل کاوه‌نگار ساخته و **تایید** شده‌اند: `crm` (کد ورود)،
`crm-forget-pass` (رمز جدید) و `crm-debt-reminder` (یادآوری بدهی).

نامشان **ثابتِ کد نیست**؛ از `.env` می‌آید (`SMS_TEMPLATE_OTP`،
`SMS_TEMPLATE_FORGET_PASSWORD`، `SMS_TEMPLATE_DEBT_REMINDER`). یک‌بار سرِ نامِ
الگوی فراموشیِ رمز ابهام پیش آمد (`crm-forget-pass` یا `crm-forget-password`) و
اگر نام داخلِ کد بود، هر تصحیحی یک کامیت می‌خواست؛ حالا یک خط در `.env` است.

> ⚠️ **بدونِ `KAVENEGAR_API_KEY` هیچ پیامکی واقعاً ارسال نمی‌شود** — حتی با
> `SMS_DEV_MODE=False`. شرطِ `_dev_mode() or not _api_key()` در `services.py` با
> کلیدِ خالی هم به شاخهٔ لاگ می‌رود، پس همه‌چیز موفق به نظر می‌رسد و در `SMSLog`
> با وضعیتِ `dev` می‌نشیند ولی گوشیِ کسی زنگ نمی‌خورد. اگر «ارسال شد» دیدید ولی
> پیامکی نرسید، اول همین کلید را ببینید.

> ⚠️ **`token` و `token2` و `token3` فاصله نمی‌پذیرند** (خطای ۴۳۱ کاوه‌نگار). هر
> مقداری که فاصله دارد — مثل نامِ کسب‌وکار «سوپرمارکت رضا» — باید در `token10`
> یا `token20` برود. `send_token_sms` حالا با `**extra_tokens` هر دو را می‌پذیرد.

**گیرندهٔ آزمایشی.** `SMS_TEST_RECIPIENT` در `.env`: اگر پر باشد **هر** پیامکی به
همان یک شماره می‌رود، ولی متنش با دادهٔ گیرندهٔ اصلی ساخته می‌شود و شمارهٔ اصلی
هم در `SMSLog` ثبت می‌ماند — وگرنه لاگ می‌گفت همهٔ پیامک‌ها برای یک نفر بوده.
لازم است چون کاوه‌نگار فقط به شماره‌های تاییدشدهٔ صاحبِ حساب ارسال دارد و
شماره‌های دفترِ آزمایشی همه ساختگی‌اند.

این بازهدایت **جای بررسیِ «این شماره در سیستم هست؟» را نمی‌گیرد**: هر دو
endpointِ ورود با پیامک و فراموشیِ رمز، قبل از هر ارسالی شماره را در دیتابیس
می‌جویند و اگر نبود ۴۰۴ می‌دهند. دو تست همین را قفل می‌کنند.

**گام ۳ — اپ `home` و مدل‌های دامنه** ✅ (۲۰۲۶-۰۸-۲۰)
`Customer`، `CustomerOwner` (با `unique_together`)، `Transaction` با فیلدهای شمسیِ
`year/month/day`. ۳۲ مشتری، ۳۲ پیوند و ۴۳ تراکنش با حفظ شناسه از `legacy_*` کپی شدند.
دو تغییرِ عمدی نسبت به پروژهٔ قدیمی:
- ستون متنیِ `status` حذف شد؛ با `code` تکراری بود و می‌توانست از آن واگرا شود.
  برچسب فارسی از `get_code_display()` می‌آید.
- `Transaction.owner` دیگر `null` نمی‌پذیرد، و `created` به‌جای `auto_now_add` از
  `default=timezone.now` استفاده می‌کند تا مقدارش داخل `save()` در دسترس باشد و
  تاریخ شمسی از روی همان مقداری ساخته شود که در ردیف می‌نشیند.

**گام ۴ — API مشتریان** ✅ (۲۰۲۶-۰۸-۲۰)
فهرست با صفحه‌بندی، `?query=` روی نام و شماره، `?filter=debt|credit|zero` + کراد.
اسکوپینگ با `OwnerScopedMixin` در **هر** ویو انجام می‌شود، نه در فیلتر فرانت؛
عوض کردن id در یوآرال ۴۰۴ می‌گیرد (تست شده).

**گام ۵ — API تراکنش‌ها** ✅ (۲۰۲۶-۰۸-۲۰)
فهرست به‌ازای مشتری (**بدون صفحه‌بندی** — چون مانده کنارش نمایش داده می‌شود و
صفحه‌بندی یعنی مانده‌ای که فقط بخشی از حساب را می‌گوید)، فیلترِ دوره، جستجوی تاریخ
با POST، و کراد. مانده همیشه روی کلِ حساب حساب می‌شود نه روی نتیجهٔ فیلتر.
بازمحاسبهٔ وضعیت در `home/services.py` با یک کوئریِ aggregate و داخل همان تراکنشِ
دیتابیسیِ تغییر. بازهٔ **دایره‌ای** ماه/روز حفظ شده — `.claude/architecture.md` §۳.

**گام ۶ — اسکلت احراز هویتِ فرانت** ✅ (۲۰۲۶-۰۸-۲۰)
`AuthProvider` + `useAuth`، گاردهای `ProtectedRoute`/`PublicOnlyRoute`/`AuthRedirect`،
و صفحه‌های ورود/ثبت‌نام/ورود با پیامک/فراموشی رمز با ظاهرِ SAM و اعتبارسنجی `zod`.

> ⚠️ **روی فرم ورود حداقلِ طول رمز نگذارید.** رمز کاربران قدیمی ۳ کاراکتر است و
> قاعدهٔ «حداقل ۴» آن‌ها را برای همیشه بیرون نگه می‌داشت. حداقلِ طول فقط جای
> *ساختنِ* رمز است (ثبت‌نام و تغییر رمز)، نه *بررسیِ* رمزی که از قبل وجود دارد.

همهٔ ولیدیتورها در یک فایل `validators/auth.js` جمع‌اند تا قاعدهٔ شماره همراه در چند
نسخهٔ واگرا تکرار نشود (در SAM شش بار کپی شده بود).

**گام ۷ — صفحهٔ مشتریان** ✅ (۲۰۲۶-۰۸-۲۰)
هدر و فوتر (هر دو در `components/common/`) به `RootLayout` اضافه شدند و جدول مشتریان
با `grid-customer`، فیلترِ وضعیت، صفحه‌بندی و مودالِ ساخت/ویرایش/حذف ساخته شد.

قراردادهایی که اینجا تثبیت شدند:
- **یوآرال منبعِ حقیقت است.** `?query=` و `?filter=` و `?page=` در یوآرال‌اند، پس
  رفرش و دکمهٔ back درست کار می‌کنند و جستجوی فوتر هم از همین راه روی صفحه اثر می‌گذارد.
- **مودالِ «ثبت مشتری جدید» در `RootLayout` رندر می‌شود، نه در صفحهٔ مشتریان.**
  دکمه‌اش در هدر است و در همهٔ صفحه‌ها دیده می‌شود، پس باید همان‌جا که کاربر
  هست باز شود و او را به صفحهٔ دیگری پرت نکند. هدر همسایه‌اش است و با پراپ
  صدایش می‌زند؛ فراخوان‌های دورتر (مثل حالتِ خالیِ داشبورد) رویدادِ
  `crm:open-new-customer` می‌فرستند و بعد از ثبتِ موفق، `crm:customer-created`
  به صفحهٔ مشتریان می‌گوید فهرست و شاخص‌هایش را تازه کند. نامِ رویدادها در
  `lib/events.js` است.
  > ⚠️ پیش‌تر این دکمه به `‎/customers?new=1‎` می‌رفت و جدول با مقایسه در حین رندر
  > بازش می‌کرد — ولی آن مقایسه هرگز روی **mountِ تازه** برقرار نمی‌شد
  > (`useState(wantsNew)` مقدارِ اولیه را برابرِ همان می‌گذاشت)، پس از هر صفحهٔ
  > دیگری فقط به فهرستِ مشتریان می‌رفت و مودالی باز نمی‌شد.
- **یک مودال برای هر سه کارِ کراد** با پراپ `mode`. سه کامپوننت جدا یعنی سه نسخهٔ
  واگرا از همان فرم — همان چیزی که در پروژهٔ قدیمی اتفاق افتاده بود.
- **ریست فرمِ مودال با `key` انجام می‌شود نه با افکت.** قاعدهٔ
  `react-hooks/set-state-in-effect` (پلاگین نسخهٔ ۷، سازگار با کامپایلر) هر
  `setState` همگام داخل بدنهٔ افکت را رد می‌کند. برای «state مشتق از props» هم
  الگوی مقایسه در حین رندر به کار می‌رود، نه افکت.
- **سرستون جدول داخل همان ناحیهٔ اسکرول است** و `sticky top-0` دارد؛ اگر بیرون بماند
  در نمای باریک با اسکرول افقی از ستون‌ها جدا می‌افتد.

**گام ۸ — صفحهٔ تراکنش‌ها** ✅ (۲۰۲۶-۰۸-۲۲)
جدول با `grid-transaction`، مانده و وضعیت حساب در فوترِ کارت، فیلترِ دوره، جستجوی
تاریخ شمسی، و مودال‌های کراد. رنگِ این صفحه **سبز** است (`btn-greenish` /
`input-greenish` / `--shadow-var-shadow-08`)، دقیقاً مثل `CustomerManagement` که
صفحهٔ مشتریان را آبی و صفحهٔ تراکنش‌ها را سبز کرده بود.

قراردادهایی که اینجا تثبیت شدند:
- **دفترِ هر مشتری مسیرِ خودش را دارد** — `/customers/<id>/transactions`. (تا گام ۸.۵
  یک پارامترِ `?customer=<id>` روی صفحهٔ مشتریان بود؛ حالا مسیرِ مستقل است چون
  شناسه بخشی از هویتِ صفحه است نه یک حالتِ اختیاری.) نامِ مشتری از پاسخِ خودِ
  سرور می‌آید نه از پراپ، پس رفرشِ صفحه و لینکِ مستقیم هم کار می‌کند.
- **نوارِ جستجوی فوتر با همان مسیر عوض می‌شود**: در دفترِ یک مشتری به «جستجوی تاریخ
  تراکنش» تبدیل می‌شود، وگرنه «جستجوی مشتری» است. نتیجه با رویدادِ سراسری
  `crm:transaction-search` به جدول می‌رسد — همان الگوی `crm:new-customer`، چون فوتر
  در `RootLayout` است و جدول فرزندِ `Outlet`.
- **فیلترِ دوره و جستجوی تاریخ جایگزین یکدیگرند.** endpointِ جستجو پارامترِ دوره
  نمی‌گیرد؛ نگه‌داشتنِ هم‌زمانِ هر دو یعنی نمایشِ چیزی که سرور نگفته.
- **یک کامپوننتِ `DateSearchField` برای هر سه جزءِ تاریخ.** در پروژهٔ قدیمی چهار
  کامپوننتِ تقریباً یکسان و حدود ۲۰۰۰ خط بود. تفاوتِ سال با ماه/روز فقط شکلِ ورودی
  است، که با پراپ `options` بیان می‌شود.
- **قرارداد دادهٔ جستجو در `dateSearch.js` است، نه کنارِ کامپوننت.** قاعدهٔ
  `react-refresh/only-export-components` اجازه نمی‌دهد یک فایل هم کامپوننت صادر کند
  هم تابع (ثابتِ رشته‌ای مثل نامِ رویداد استثناست).
- **قاعدهٔ «مقدارِ ۱ را از یوآرال بردار» فقط مالِ `page` است.** پیش‌تر عمومی بود و
  `?customer=1` را هم پاک می‌کرد؛ یعنی مشتریِ با شناسهٔ ۱ هرگز باز نمی‌شد.
- **پنلِ جستجو `z-[5]` است** تا فوترِ `z-10` در حالت بسته پنهانش کند، و `inert`
  می‌گیرد نه `aria-hidden` — با دومی دکمه‌هایش هنوز با Tab در دسترس بودند.
- **مانده و وضعیت همیشه روی کلِ حساب‌اند**، حتی وقتی فیلترِ دوره فعال است.
- **ارتفاعِ جدول به اندازهٔ پنج ردیف بسته است** (`.h-transactions-5`). چون فهرست
  صفحه‌بندی ندارد، «پنج‌تایی» با max-height ساخته می‌شود نه با page_size.

بررسی‌شده با کلاینتِ تستِ جنگو روی دادهٔ واقعی: فهرست، پنج فیلترِ دوره، هفت شکلِ
جستجو (شاملِ بازهٔ دایره‌ای و ارقام فارسی)، کرادِ کامل با بازمحاسبهٔ مانده، و پنج
سناریوی IDOR (همه ۴۰۴).

**گام ۸.۵ — ساختارِ صفحه‌ها** ✅ (۲۰۲۶-۰۸-۲۴)
اپ از یک صفحهٔ مشتریان به پنج مقصد باز شد: `/home` (داشبورد با سایدبار و مینِ
فعلاً خالی؛ نمودارها بعداً)، `/customers`، `/customers/:customerId/transactions`،
`/all-transactions` و `/chat`. سایدبار فقط در داشبورد است — صفحه‌های جدول ۹۰٪
عرضِ صفحه‌اند و سایدبار ندارند، پس هر کدام یک **دکمهٔ بازگشت** در راستِ هدرِ
کارت دارند که با `useGoBack()` به صفحهٔ قبل برمی‌گردد (نه به یک مقصدِ ثابت؛
فقط اگر همین صفحه اولین ورودیِ تاریخچه باشد به مقصدِ پشتیبان می‌رود).

هر صفحه‌ای جز داشبورد **نوارِ مسیر** (`components/common/Breadcrumb.jsx`) بالای
کارتش دارد. کامپوننت خودش «داشبورد» را به‌عنوان ریشه می‌گذارد و صفحه فقط ادامهٔ
مسیر را می‌دهد؛ آخرین حبه لینک نمی‌شود و `aria-current="page"` می‌گیرد.

- **مسیرهای پرتکرار در `lib/paths.js`اند** — هم سازندهٔ `customerLedgerPath(id)`
  برای `navigate` و هم الگوی `CUSTOMER_LEDGER_PATTERN` برای `useMatch` در فوتر.
  فوتر مسیرِ پارامتردار را با `useMatch` تشخیص می‌دهد نه با مقایسهٔ رشته‌ای.

قراردادهایی که اینجا تثبیت شدند:
- **`/all-transactions` جدولِ همهٔ تراکنش‌های مالک است، با اسکرولِ بی‌نهایت.**
  `AllTransactionsView` (`select_related("customer")`) و `AllTransactionsSearchView`
  برای جستجوی تاریخ. مانده اینجا نمایش داده نمی‌شود، پس برخلافِ دفترِ یک مشتری
  صفحه‌بندی مشکلی نمی‌سازد.
- **اندازهٔ دسته از روی گنجایشِ کادر حساب می‌شود، نه یک عددِ ثابت.**
  `computePageSize()` ارتفاعِ viewport را می‌گیرد و `page_size` را می‌فرستد، پس
  با ده‌هزار تراکنش هم بارِ اول فقط چند ردیفی که دیده می‌شود از دیتابیس خوانده
  می‌شود. سرور تا سقفِ ۱۰۰ می‌پذیرد (`page_size_query_param`).
  > ⚠️ **`pageSize` بعد از اولین دسته قفل می‌شود.** شماره‌صفحه با اندازهٔ متغیر
  > معنا ندارد: صفحهٔ ۲ با اندازهٔ دیگر، ردیف‌های بینِ دو اندازه را جا می‌اندازد.
  > تغییرِ اندازهٔ پنجره وسطِ کار روی همان فهرست اثر نمی‌گذارد؛ فقط ریست (تغییرِ
  > فیلتر یا جستجو) دوباره اندازه می‌گیرد.
  > همچنین `PREFETCH_ROWS` باید **کمتر از** `PAGE_BUFFER_ROWS` بماند، وگرنه
  > بلافاصله بعد از دستهٔ اول دستهٔ دوم هم گرفته می‌شود و کلِ ماجرا بی‌اثر است.
- **این جدول اسکرول‌بار ندارد** (`hideScrollbars` روی `ScrollContainer`، که
  `scrollbars.visibility: "hidden"` را ست می‌کند). نشانهٔ «ادامه دارد» **مه‌آلودیِ
  پایینِ کارت** است که با رسیدن به ته فهرست محو می‌شود؛ دو نشانه برای یک چیز نویز است.
- **فقط ردیف‌های صفحهٔ آخر انیمیشنِ ورود می‌گیرند.** `freshFrom` در همان state
  فهرست نگه داشته می‌شود (اندیسِ اولین ردیفِ آخرین صفحه) و `animate-fade-up` با
  تاخیرِ پلکانیِ سقف‌دار روی همان‌ها می‌نشیند — وگرنه با هر صفحهٔ تازه کلِ جدول
  دوباره ظاهر می‌شد.
- **نوارِ جستجوی فوتر سه حالت دارد، نه دو:** در `/home` اصلاً نیست، در
  `/all-transactions` و در دفترِ یک مشتری جستجوی تاریخ است، و جای دیگر جستجوی مشتری.
- **جستجوی تاریخ در یوآرال نمی‌نشیند** (بدنهٔ POST دارد)، پس در `/all-transactions`
  کلیدِ ریستِ فهرست علاوه بر `?query=` و `?tfilter=` شاملِ خودِ payload هم هست.
- **صفحهٔ ۴۰۴ هدر و فوتر ندارد** (`handle: {chrome: false}`)، پس پس‌زمینه و
  چیدمانِ تمام‌صفحه‌اش را — مثل صفحه‌های احراز هویت — خودش می‌سازد.

**گام ۸.۷ — داشبورد و تحلیل** ✅ (۲۰۲۶-۰۸-۲۵)
مینِ خالیِ `/home` پر شد: چهار کارتِ KPI، سه نمودار (روندِ ترکیبی، دوناتِ ترکیبِ
مشتریان، میله‌ایِ سررسیدِ بدهی)، یک جدول و شش فهرست/کارتِ تحلیلی. کتابخانهٔ نمودار
**recharts** است و کلِ داشبورد با `React.lazy` جدا بارگذاری می‌شود — چانکش
حدود ۱۲۶KB gz است و نباید روی صفحهٔ ورود بنشیند.

- **یک endpoint برای همه‌چیز**: `GET /api/dashboard/?period=today|week|month|year|all`
  (پیش‌فرض `year`). منطقش در `home/dashboard.py` است، نه در ویو — همان جدایی که
  `services.py` دارد. کلِ پاسخ حدود ۷ کوئری است.
- **همه‌چیز اسکوپ‌شده به مالکِ درخواست.** وضعیتِ حساب اینجا از تراکنش‌های همین
  مالک حساب می‌شود و **نه** از ستونِ کش‌شدهٔ `Customer.code` — آن ستون یکی است
  برای همهٔ مالکانِ یک مشتری.
- **«بی‌حساب» به دو بخش شکسته شد**: «تسویه‌شده» و «بدون تراکنش». در دادهٔ امروز
  ۲۴ مشتری بی‌حسابند که ۲۳تایشان اصلاً معامله‌ای نکرده‌اند — یکی شمردنشان
  گمراه‌کننده بود.
- **تعریفِ «خوش‌حساب» صریح است**: مشتری‌ای که نسیه گرفته و بیشترین نسبتِ پرداخت
  به نسیه را دارد؛ نسبت سقفِ ۱۰۰٪ دارد و در تساوی مبلغِ بزرگ‌تر جلو می‌افتد.
  سه نفرِ اول مدالِ طلا/نقره/برنز می‌گیرند.
- **دلتا فقط وقتی نشان داده می‌شود که دورهٔ قبل عددی داشته باشد.** با پایهٔ صفر،
  درصدِ تغییر بی‌معناست و عددِ ساختگی بدتر از نبودنش است.
- **انیمیشنِ تغییر با `key` انجام می‌شود**: با هر بازخوانیِ موفق یک شمارندهٔ
  `revision` جلو می‌رود و کلیدِ گریدِ کاشی‌هاست، پس کلِ داشبورد دوباره mount و
  انیمیشنِ ورودِ پلکانی و انیمیشنِ نمودارها از نو پخش می‌شود. حینِ بارگذاری
  داده‌های قبلی محو می‌شوند نه حذف — صفحه نمی‌پرد.
- **توکن‌های نمودار در `index.css`** (`--chart-axis`، `--chart-grid`، …) چون
  recharts رنگ را به‌صورت صفتِ SVG می‌گیرد و کلاسِ تیلویند رویش نمی‌نشیند.
  رنگ‌های سری در `lib/chart.js` یک‌جا تعریف شده‌اند تا «نسیه» در هر چهار نمودار
  یک رنگ بماند.
- **RTL در نمودارها**: محورِ افقی `reversed` و محورِ عمودی `orientation="right"`،
  وگرنه نمودار برعکسِ جهتِ خواندنِ صفحه پیش می‌رود.

**دادهٔ نمایشی.** `home/management/commands/seed_demo.py` یک دفترِ یک‌سالهٔ
شبیه‌سازی‌شده می‌سازد (سه پروفایلِ رفتاری: خوش‌حساب/معمولی/بدحساب) تا داشبورد در
حالتِ پُر دیده شود:

```bash
venv/bin/python manage.py seed_demo --owner 09xxxxxxxxx --customers 60 --months 14
```

بدونِ `--clear` چیزی حذف نمی‌شود. `--clear` فقط دادهٔ **همان مالک** را پاک می‌کند
و تاییدِ تعاملی می‌خواهد.

**گام ۹ — پالایش و تست** ✅ (۲۰۲۶-۰۸-۲۵)
**۱۵۴ تست** در `home/tests/` و `account/tests.py`. با `venv/bin/python manage.py test`
اجرا می‌شوند و روی دیتابیسِ تستِ جداگانه کار می‌کنند، پس دفترِ واقعی دست نمی‌خورد.

| فایل | چه چیزی را می‌سنجد |
|---|---|
| `home/tests/test_scoping.py` | IDOR روی **هر** endpointِ دامنه + دسترسیِ مهمان |
| `home/tests/test_customers_api.py` | فهرست، فیلتر، جستجو، صفحه‌بندی و کراد |
| `home/tests/test_transactions_api.py` | دفتر، فیلترِ دوره، جستجوی تاریخ، اسکرولِ بی‌نهایت |
| `home/tests/test_stats_api.py` | داشبورد و سه نوارِ شاخص |
| `home/tests/test_services.py` | واحدهای `services.py` بدونِ HTTP |
| `home/tests/test_reports.py` | صورتحساب و خروجیِ اکسل |
| `home/tests/test_reminders.py` | فهرستِ بدهکاران، ارسال، بازهٔ خاموشی |
| `home/tests/test_seed_demo.py` | دستورِ دادهٔ نمایشی |
| `account/tests.py` | ثبت‌نام، ورود، کد یکبارمصرف، تغییر و فراموشیِ رمز |

قراردادهایی که تست‌ها **قفلشان می‌کنند** — اگر روزی شکستند، رفتار عوض شده نه تست:
- **مالکِ دیگر ۴۰۴ می‌گیرد، نه ۴۰۳.** ۴۰۳ خودش نشتِ اطلاعات است: می‌گوید «این
  شناسه هست ولی مالِ تو نیست». مهمان هم روی هیچ endpointِ دامنه‌ای پاسخ نمی‌گیرد.
- **مانده و شاخص‌ها به فیلترِ دوره وابسته نیستند.** سه تستِ جدا همین را می‌سنجند،
  چون دقیقاً همان چیزی است که در پروژهٔ قدیمی خراب بود.
- **کدِ یکبارمصرف و رمزِ تازه هرگز در `SMSLog` نمی‌نشینند.**
- **رمزِ سه‌کاراکتریِ کاربرانِ قدیمی همچنان وارد می‌شود** — حداقلِ طول فقط جای
  *ساختنِ* رمز است، نه *بررسیِ* رمزِ موجود.
- **بازهٔ دایره‌ای ماه/روز** (۱۱ تا ۲ یعنی بهمن تا اردیبهشت) و **ارقام فارسی**.

> ⚠️ `MyUser.otp` یک `PositiveIntegerField` است نه رشته؛ در تست با `str()` مقایسه‌اش
> کنید. و `‎/api/auth/me/‎` برای مهمان **۲۰۰ با بدنهٔ خالی** می‌دهد نه ۴۰۱ — «لاگین
> نیستی» خطا نیست — پس `response.json()` آنجا می‌شکند و باید `response.content` را دید.

**بازبینیِ دو تم** با یک اسکنِ خودکار روی همهٔ رشته‌های `className` انجام شد: هر
رنگِ مخصوصِ یک تم که جفتِ `dark:` نداشت بیرون کشیده شد. تنها ایرادِ واقعی، متنِ
**سفید روی دکمهٔ فیروزه‌ای** در دو دکمهٔ صفحهٔ گفتگو بود (کنتراستِ ۱٫۹ که رد
می‌شود)؛ به همان `text-var-color-11` بقیهٔ دکمه‌ها تغییر کرد.

**بازبینیِ ریسپانسیو**: نوارهای KPI سه صفحه `grid-cols-2` بودند و روی موبایل هر
ستون ~۱۶۵ پیکسل می‌شد که عددِ میلیونی در آن جا نمی‌شود؛ به
`grid-cols-1 2xs:grid-cols-2 xl:grid-cols-4` تغییر کردند — همان الگوی نوارِ داشبورد.

**دادهٔ نمایشی**: دستور آماده و تست‌شده است ولی **روی دفترِ واقعی اجرا نشده** —
انتخابِ مالک با شماست.

**گام ۱۰ — تکمیل «به‌زودی»ها** 🔸 (۲۰۲۶-۰۸-۲۶ — پنج مورد از شش)
**راهنمای نرم‌افزار**، **پروفایل**، **صورتحساب مشتریان**، **خروجی اکسل** و
**یادآوری پیامکی به بدهکاران** پیاده شدند. باقی‌مانده: **ورود با گوگل**.

> 📌 **«سرمایه در گردش» از منو حذف شد.** آن اسم و «صورتحساب همه مشتریان» هر دو
> از پروژهٔ قدیمی کپی شده بودند، ولی آنجا هم هیچ‌وقت پیاده نشده بودند — در
> `Footer.jsx`ِ قدیمی فقط `href="#money"` و `href="#bill"` بودند. سرمایه در
> گردش (دارایی جاری منهای بدهی جاری) از این داده **قابل محاسبه نیست**: دفتر نه
> نقدِ صندوق را می‌داند نه موجودی نه بدهیِ مالک به دیگران. نزدیک‌ترین عددِ
> محاسبه‌شدنی همان «طلبِ کل» است که از قبل در داشبورد و بالای جدولِ مشتریان
> نشان داده می‌شود، و نسخهٔ سومش ارزشی اضافه نمی‌کرد.

**راهنما** یک مودالِ شش‌گامی است (`components/common/GuideModal.jsx`) که ترتیبِ
واقعیِ کار را می‌گوید نه فهرستِ قابلیت‌ها: ثبت مشتری ← ثبت نسیه/پرداخت ← خواندنِ
مانده ← جستجو ← داشبورد ← پیگیری. شماره‌گذاری اینجا تزئین نیست، همان توالیِ کار است.
با کلیدهای جهت‌دار هم جابه‌جا می‌شود و Esc می‌بندد.

**پروفایل** صفحهٔ `/profile` است: کارتِ هویت با آپلودِ تصویر، فرمِ نام و آدرس،
دکمهٔ تغییرِ رمز (همان مودالِ موجود)، و خلاصهٔ دفتر که از همان دو endpointِ
شاخصِ صفحه‌های دیگر می‌آید — نه کوئریِ تازه‌ای لازم شد نه endpointِ تازه‌ای.

- **`PATCH /api/auth/profile/`** با `ProfileUpdateSerializer`. شناسه نمی‌گیرد و
  همیشه روی `request.user` کار می‌کند، پس اصلاً IDOR ممکن نیست.
  > ⚠️ **`phone` عمداً در سریالایزرِ ویرایش نیست.** همان `USERNAME_FIELD` است و
  > عوض کردنش یعنی عوض شدنِ هویتِ ورود — فلوی خودش را می‌خواهد (تاییدِ شمارهٔ
  > تازه با کد یکبارمصرف)، نه یک فیلدِ ساده در فرم. صفحه شماره را فقط نشان می‌دهد.
  > `role` هم به همین دلیل نیست: با بودنش، کاربر می‌توانست خودش را superuser کند.
- **`MultiPartParser` روی همین ویو لازم است** — پیش‌فرضِ پروژه فقط JSON است و
  بدونش آپلودِ تصویر با ۴۱۵ رد می‌شود. فرانت هم فقط وقتی تصویر هست `FormData`
  می‌فرستد؛ بدونش JSONِ سبک‌تر می‌رود.

**صورتحساب** صفحهٔ `/statement` است با `chrome: false` — یک **سند**، نه صفحهٔ
دیگری از برنامه. هدر و فوترِ اپ روی کاغذ معنایی ندارند و خودِ سند سربرگِ خودش
(لوگو، نامِ مالک، تاریخ شمسی) را دارد.

> ⚠️ **چاپ با `window.print()` انجام می‌شود، نه PDFِ سمتِ سرور.** ساختنِ PDFِ
> فارسیِ راست‌به‌چپ روی سرور دردسرِ فونت و شکستِ حروف دارد، در حالی که خودِ
> مرورگر همین صفحه را بی‌نقص چاپ می‌کند و کاربر از همان دیالوگ «ذخیره به‌صورت
> PDF» را هم دارد. پس هم چاپ داریم هم PDF، بدونِ هیچ وابستگی. قاعده‌های `print:`
> نوارِ ابزار را حذف می‌کنند و `break-inside-avoid` نمی‌گذارد ردیفی وسطِ چاپ
> بین دو صفحه بشکند.

**خروجی اکسل** یک `.xlsx` واقعی با **openpyxl** است (نه CSV)، با دو برگهٔ
«مشتریان» و «تراکنش ها». نقشش پشتیبانِ خودِ کاربر است، پس همهٔ ردیف‌ها می‌روند
نه خلاصه. منطقش در `home/reports.py` است — جدا از `dashboard.py` چون آنجا چند
عددِ خلاصه برای دیدن ساخته می‌شود و اینجا کلِ دفتر برای بیرون بردن.

- ⚠️ **`sheet_view.rightToLeft = True` لازم است**، وگرنه اکسل ستون‌ها را از چپ
  می‌چیند و جدولِ فارسی وارونه به نظر می‌رسد. یک تست همین را قفل می‌کند.
- دانلود عمداً از `api/client.js` نمی‌رود (آنجا پاسخ JSON پارس می‌شود و اینجا
  بدنه باینری است). `window.location.assign` می‌رود، پس دیالوگِ ذخیره و نوارِ
  پیشرفتِ خودِ مرورگر رایگان به دست می‌آید و کوکیِ سشن چون هم‌دامنه است همراه می‌رود.

**یادآوری پیامکی به بدهکاران** (۲۰۲۶-۰۸-۲۶) از منوی فوتر باز می‌شود: یک مودالِ
انتخاب که بدهکاران را با مبلغ و روزهای گذشته از آخرین تراکنش نشان می‌دهد و کاربر
تیک می‌زند. منطقش در `home/reminders.py` است و ارتباط با کاوه‌نگار مثلِ همیشه از
`account/services.py` می‌رود؛ اینجا فقط تصمیم می‌گیرد **به چه کسی** و **چه وقت**.

متنِ الگوی `crm-debt-reminder`:

```
مشتری گرامی
مانده بدهی شما نزد %token10 مبلغ %token تومان می باشد.
لطفا جهت تسویه حساب اقدام فرمایید.
با تشکر
```

- **دو endpoint، نه یکی**: `GET /api/reminders/debtors/` و `POST
  /api/reminders/send/`. پیامک پول دارد و به آدمِ واقعی می‌رسد، پس هیچ ارسالی
  بدونِ دیدنِ فهرست انجام نمی‌شود و هیچ ردیفی پیش‌فرض تیک‌خورده نیست.
- ⚠️ **نامِ کسب‌وکار در `token10` می‌رود، مبلغ در `token`.** نام فاصله دارد و
  `token`/`token2`/`token3` فاصله نمی‌پذیرند (خطای ۴۳۱). مبلغ هم با ارقامِ
  **لاتین** و جداکنندهٔ سه‌رقمی می‌رود؛ توکنِ کاوه‌نگار روی ارقامِ فارسی سخت‌گیر است.
- **بازهٔ خاموشیِ ۲۴ ساعته** روی `SMSLog(event="debt_reminder")` حساب می‌شود، نه
  روی یک ستونِ تازه در `Customer`: لاگ از قبل همان چیزی را می‌داند که لازم است
  (به چه شماره‌ای و کِی). پیامکِ تکراری هم پول است هم مشتری را می‌رنجاند، و
  «دوبار کلیک کردن» اتفاقِ رایجی است.
- **شکستِ یکی بقیه را متوقف نمی‌کند.** با ده گیرنده، یک خطای شبکه نباید نه نفرِ
  دیگر را از دست بدهد؛ پاسخ سه فهرستِ `sent` / `skipped` / `failed` دارد.
- **اسکوپینگ اینجا با فیلتر است نه با ۴۰۴.** شناسه‌ای که بدهکارِ این مالک نباشد
  اصلاً در فهرست نیست و در پاسخ به‌عنوان «رد شده» می‌آید — چون این یک درخواستِ
  دسته‌ای است و یک شناسهٔ نامعتبر نباید نُه ارسالِ درست را باطل کند.
- **وضعیت از تراکنش‌های همین مالک حساب می‌شود، نه از `Customer.code`** — همان
  قاعدهٔ داشبورد؛ آن ستون بین همهٔ مالکانِ یک مشتری مشترک است.
- **مودالِ نامِ کسب‌وکار به مودالِ ارسال زنجیر است**: کاربر برای فرستادنِ پیامک
  آمده بود و وسطِ راه نام از او پرسیده شد، پس بعد از ذخیره همان‌جا ادامه می‌دهد
  نه اینکه از منو شروع کند (`onSaved` در `RootLayout`).
- ⚠️ **`notify` اول همهٔ پیغام‌های قبلی را می‌بندد**، پس گزارشِ «چندتا رفت و چندتا
  نرفت» باید **یک** پیغام باشد؛ دو فراخوانِ پشتِ هم یعنی کاربر فقط آخری را می‌بیند.

**تغییرِ فرم‌های احراز هویت (همان روز).** فیلدِ **آدرس از ثبت‌نام حذف شد** و جایش
صفحهٔ پروفایل است؛ در سرور هم شرطِ الزامی بودنش برداشته شد (در سطح مدل از اول
`blank=True` بود). جای خالی‌اش را **تکرار رمز عبور** گرفت، پس فرم همان چهار
ردیفِ متقارن و همان ارتفاعِ `h-150`ِ فرمِ ورود را دارد.

`repeat_password` فقط سمتِ کلاینت است و به سرور فرستاده نمی‌شود — سرور دلیلی
ندارد آن را ببیند. تطابقِ دو رمز با `.refine()` روی **خودِ فیلدِ تکرار** گزارش
می‌شود تا فوکوس جای درستی برود، همان قاعده‌ای که فرمِ تغییرِ رمز هم دارد.

> ⚠️ **فونتِ پایه روی `body` نشست، نه روی `RootLayout`.** صفحه‌های احراز هویت و
> ۴۰۴ با `handle: {chrome: false}` اصلاً از `RootLayout` رد نمی‌شوند و تا امروز
> با فونتِ پیش‌فرضِ مرورگر رندر می‌شدند.

**گام ۱۱ — دستیارِ گفتگو** ⬜
صفحهٔ `/chat` پوسته دارد ولی به هیچ داده‌ای وصل نیست؛ نقطهٔ اتصال با
`TODO(فاز توسعه)` در `pages/Chat/components/ChatPane.jsx` علامت خورده و هر پیام
همان پاسخِ ثابتِ `NOT_WIRED` را می‌گیرد. گفتگوها هم فقط در حافظهٔ همان صفحه‌اند و
مدلی سمتِ سرور ندارند.

**گام ۱۲ — استقرار** ⬜
پروژه هرگز مستقر نشده. `SECRET_KEY` و `DEBUG` و `ALLOWED_HOSTS` از `.env` می‌آیند،
ولی `STATIC_ROOT`، سروِ بیلدِ فرانت، و تنظیماتِ امنیتیِ production (`SECURE_*`،
کوکیِ امن، HSTS) هنوز نیستند. جدول‌های `legacy_*` هم همچنان در دیتابیس‌اند و
حذفشان با شماست.

## فرانت‌اند — قراردادهایی که زیاد اشتباه می‌شوند

**Tailwind نسخهٔ ۴ و CSS-first است.** هیچ `tailwind.config.js` وجود ندارد و نباید ساخته شود؛
تم در بلاک `@theme` داخل `src/index.css` تعریف شده. دارک‌مود مبتنی بر کلاس است با
`@custom-variant dark (&:where(.dark, .dark *))` که باید بلافاصله بعد از `@import` بماند.

**رنگ‌ها شماره‌دارند، نه معنایی.** `--color-var-color-00` تا `49` در `@theme`.
رنگ تازه = شمارهٔ بعدی. قبل از انتخاب رنگ، مقدارش را از `index.css` بخوانید.
پرکاربردها: `00`=سفید، `01`=#F9F9F9، `02`=#e5e7eb (بوردر استاندارد)، `15`=#00bcff (اکسنت)،
`35`=پس‌زمینهٔ دارک، `36/37`=سطوح دارک، `38`=بوردر دارک، `49`=پس‌زمینهٔ لایتِ پنل.

**RTL دو تله دارد:**
- در فلکس، `mr-auto` عنصر را به **چپ** می‌برد و `ml-auto` به **راست** — برعکسِ شهود.
  برای «انتهای خط» در RTL همیشه `mr-auto`.
- ترتیب DOM برعکس دیده می‌شود: اولین فرزند، راست‌ترین است.

**`:hover` روی دکمهٔ `disabled` هم اعمال می‌شود.** برای افکت‌های هاور روی دکمه‌های شرطی
حتماً `enabled:hover:...` بنویسید.

**کامنت JSX بین صفت‌های یک عنصر نامعتبر است** — بالای عنصر بگذارید. بیلد گاهی رد می‌شود
ولی ESLint می‌گیرد، پس فقط به «بیلد شد» اتکا نکنید.

**سایه‌ها حذف شده‌اند.** پنل‌ها فقط با رنگ و بوردر `var-color-02` از هم جدا می‌شوند.
سایه فقط برای مودال‌ها عمدی است.

**اسکرول همه‌جا با `ScrollContainer` است، نه `overflow-auto`.** کتابخانه‌اش
OverlayScrollbars است — همانی که در `CustomerManagement` به کار رفته بود — و
اسکرولِ خودِ صفحه هم در `main.jsx` روی `body` سوار می‌شود. تمِ ظاهری یک‌بار در
`index.css` تعریف شده (`.os-theme-app`) و هر نمونه فقط متغیرهای CSS را عوض می‌کند؛
در پروژهٔ قدیمی هر نمونه یک تگ `<style>` کامل تولید می‌کرد.

> ⚠️ **کلاس‌های چیدمان روی `ScrollContainer` نمی‌نشینند.** کتابخانه بینِ آن و
> فرزندانش عنصرِ viewport می‌گذارد، پس `flex`/`gap`/`p-*` باید روی یک div داخلی
> بروند و فقط کلاس‌های اندازه (`flex-1`، `min-h-0`، `max-h-*`، `h-full`) روی خودش
> بمانند. هر جا هم کد لازم دارد دستی اسکرول کند (مثل «برو به آخرِ گفتگو») باید
> `viewportRef` بگیرد، نه ref روی خودِ کامپوننت.

پیش‌فرضِ سمتِ اسکرول‌بار **چپ** است، چون در RTL چپ انتهای خط است و روی شروعِ متن
نمی‌افتد. با `position="right"` جابه‌جا می‌شود.

> ⚠️ **`ScrollTimeline` را پیش از بارگذاریِ کتابخانه کنار می‌گذاریم — این کدِ مرده
> نیست، حذفش نکنید.** در `src/lib/scrollTimelineGuard.js` (اولین ایمپورتِ
> `main.jsx`) و یک نسخهٔ دومِ پشتیبان در `index.html`.
>
> اگر overlayscrollbars این API را ببیند، حرکتِ دستهٔ اسکرول را با یک انیمیشنِ
> Web Animations اداره می‌کند و `--os-scroll-percent` را **اصلاً نمی‌نویسد**. آن
> انیمیشن هنگام بازساختِ اسکرول‌بارها لغو می‌شود ولی چون کی‌فریمِ کش‌شده عوض نشده،
> دوباره ساخته نمی‌شود — و **دسته بالای جدول قفل می‌ماند**. بازساخت با
> ResizeObserver و MutationObserver هم رخ می‌دهد، پس از بیرون نمی‌شود جلویش را
> گرفت. نسخهٔ ۲.۱۶.۰ آخرین نسخه است، پس ارتقا راهی نیست.
>
> در `main.jsx` بلافاصله بعد از ایمپورت‌ها گلوبال برگردانده می‌شود.

**لایهٔ دوم — و مهم‌ترش: `lib/scrollSync.js`.** جای دستهٔ اسکرول دیگر اصلاً به
کتابخانه سپرده نشده. `ScrollContainer` و نمونهٔ `body` روی رویدادِ عمومیِ `scroll`
خودشان `transform: translateY(...)` را با حسابِ پیکسلی روی دسته می‌نویسند:
`درصدِ اسکرول × (ارتفاعِ ریل − ارتفاعِ دسته)`.

> ⚠️ **چرا این لازم است — هر دو مسیرِ کتابخانه در عمل شکستند، هر کدام در یک مرورگر:**
> - **کروم:** `ScrollTimeline` دارد، پس کتابخانه حرکت را با انیمیشنِ Web Animations
>   اداره می‌کند؛ آن انیمیشن با بازساختِ اسکرول‌بارها لغو می‌شود و دیگر ساخته نمی‌شود.
> - **فایرفاکس:** `ScrollTimeline` ندارد، پس مسیرِ CSS می‌رود:
>   `translateY(calc(pct * 100cqh + pct * -100%))`. این زنجیره به `@property`،
>   واحدهای container query و `container-type: size` وابسته است و آنجا دسته را
>   تکان نمی‌دهد.
>
> نوشتنِ پیکسلیِ ما به هیچ‌کدامِ این‌ها وابسته نیست. اگر انیمیشنِ کتابخانه زنده و
> سالم باشد، انیمیشن روی `transform` بر استایلِ درون‌خطی می‌چربد و کدِ ما بی‌اثر
> است — یعنی جایی را خراب نمی‌کند، فقط حفره‌ها را پر می‌کند.
>
> با آزمایش تایید شد: ریل ۲۱۴، دسته ۶۰، بردِ ۱۵۴ پیکسل — و مقدارِ نوشته‌شده در
> هر نقطه دقیقاً با فرمول جور درآمد، هم با انیمیشنِ زنده هم با انیمیشنِ لغو‌شده.

> 📌 **تلهٔ عیب‌یابی:** اگر باز هم گزارشِ «اسکرول‌بار ثابت است» گرفتید، **اول
> مرورگر را بپرسید.** سه دور از این ماجرا صرفِ اصلاحِ مسیرِ کروم شد در حالی که
> گزارش‌دهنده روی فایرفاکس بود و اصلاً آن مسیر را نمی‌رفت. یک خط برای تشخیص:
> `'ScrollTimeline' in window` — اگر false بود، مرورگر مسیرِ CSS را می‌رود.

> ⚠️ **قابلیت‌های تازهٔ CSS را برای چیزهای حیاتی به کار نبرید.** این تله دو بار
> زده: یک‌بار اسکرول‌بار، یک‌بار کشوهای جستجوی تاریخ که با ترنزیشنِ
> `grid-template-rows: 0fr → 1fr` باز می‌شدند و در فایرفاکس اصلاً باز نمی‌شدند —
> کاربر فکر می‌کرد جستجو خراب است. هر جا باز/بسته شدن یا جابه‌جایی «کار کردنِ»
> یک قابلیت است نه زینتِ آن، مقدار را با پیکسل بنویسید (ارتفاع را اندازه بگیرید،
> `max-height` بدهید) نه با `fr` و `cqh` و `@property`.

> 📌 **تلهٔ آزمایش در مرورگرِ داخلی:** پنلِ مرورگرِ این ابزار `visibilityState`
> را `hidden` گزارش می‌کند، و کروم در تبِ پنهان `requestAnimationFrame`،
> `ResizeObserver` و **تایم‌لاینِ انیمیشن‌ها** را معلق می‌کند. نتیجه‌اش این است که
> یک ترنزیشنِ در حال اجرا با `currentTime: 0` گیر می‌کند و مقدارِ شروع را قفل
> می‌کند — حتی روی استایلِ درون‌خطی هم می‌چربد. چند بار همین باعث شد چیزِ سالمی
> «خراب» به نظر برسد. برای سنجش، مقدارِ **درون‌خطی** (`el.style.x`) را بخوانید نه
> `getComputedStyle`، یا با `instance.update(true)` زمان‌بند را دور بزنید.

## بک‌اند — نکات

- پیش‌فرضِ DRF در این پروژه `IsAuthenticated` است؛ endpointهای عمومی خودشان
  `permission_classes = [AllowAny]` می‌گذارند.
- خطاهای DRF با کلید `detail` می‌آیند؛ در فرانت اول آن را بخوانید بعد `message` و `fieldErrors`.
- `parser` فقط JSON است؛ برای آپلود فایل باید `MultiPartParser` را روی همان ویو اضافه کنید.

## سبک کد

کامنت‌ها فارسی‌اند و «چرا» را توضیح می‌دهند نه «چه». همین سبک را ادامه دهید.

هر تغییرِ تمام‌شده را خودتان روی `main` کامیت **و پوش** کنید؛ پیام کوتاه و انگلیسی، با
تریلر `Co-Authored-By: Claude <noreply@anthropic.com>`. ریموت:
`https://github.com/farhadsaeidi/CRM.git` — احراز هویت با `credential.helper=store`
(فایل `~/.git-credentials` با مجوز ۶۰۰) که همان اعتبارنامه برای SAM و CRM کار می‌کند،
پس هیچ توکنی لازم نیست جایی تایپ شود.

قبل از کامیت: `npx eslint src/` و `npm run build` در `frontend/`، و
`venv/bin/python manage.py check` در ریشه.

> ⚠️ **سرورهای توسعه را اجرا و متوقف نکنید و سراغ ابزار مرورگر نروید.** صاحبِ
> پروژه خودش `npm run dev` و `runserver` را بالا نگه می‌دارد؛ بستنِ پورت ۵۱۷۳ یا
> ۸۰۰۰ یعنی او باید دوباره دستی راه بیندازد، و هر فراخوانیِ مرورگر یک پرامپتِ
> تایید می‌سازد که باید پای سیستم بزند. تاییدِ تغییرات با همان لینت و بیلد بالا
> انجام می‌شود و در صورت نیاز grep روی `frontend/dist/assets/*.css`. اگر چیزی را
> بدونِ دیدن نمی‌شود تایید کرد، همان را صریح بگویید. جزئیات در
> `.claude/memory/user-working-style.md`.

قبل از پوش: مطمئن شوید `.env` ردیابی نمی‌شود (`git ls-files | grep '\.env$'` باید خالی
باشد) و چیزی حساس وارد کامیت نشده. بازنویسی تاریخچهٔ پوش‌شده (force-push یا ریبیسِ
کامیت‌های پوش‌شده) بدون پرسیدن انجام نشود.

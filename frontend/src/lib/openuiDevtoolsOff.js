// ⚠️ باید پیش از اولین ایمپورتِ `@openuidev/react-lang` اجرا شود — در `main.jsx`
// درست بعد از `scrollTimelineGuard` است. این کدِ مرده نیست؛ حذفش نکنید.
//
// آن بسته در حالتِ dev یک ابزارِ توسعهٔ شناور را خودکار سوار می‌کند که کدِ خودش
// را هنگامِ اجرا از یک CDN دانلود می‌کند: کدِ بیرونی که بی‌اجازه داخلِ صفحه اجرا
// می‌شود، با دکمه‌ای که درست روی دکمهٔ لوگوی فوتر (گوشهٔ پایین‌راست) می‌افتاد.
//
// این نماد علامتِ خودِ کتابخانه برای «قبلاً سوار شده» است؛ اگر از پیش ست باشد
// هیچ کاری نمی‌کند. در بیلدِ production کلِ آن کد حذف می‌شود و این خط بی‌اثر است.
globalThis[Symbol.for("openui.devtools.autoMount")] = true;

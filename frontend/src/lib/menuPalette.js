/**
 * رنگ‌بندیِ مشترکِ ردیفِ منوها — فیلترِ مشتریان و دوره، دراپ‌داونِ فوتر، منوی
 * تنظیمات، کشوی انتخاب رنگ، کشوی مدل و منوی سه‌نقطهٔ گفتگو.
 *
 * ⚠️ **همهٔ منوها باید یک‌شکل هاور شوند.** پیش‌تر هرکدام رنگِ خودش را داشت —
 * یکی زمینهٔ پُرِ فیروزه‌ای، یکی خاکستریِ روشن، یکی سرمه‌ای — و چون در فوتر
 * کنارِ هم باز می‌شوند، ناهماهنگی‌شان مستقیم دیده می‌شد. رشته‌ها اینجا یک نسخه
 * دارند و هر منو فقط وصلشان می‌کند.
 *
 * 📌 **مرجعش دکمهٔ «ثبت مشتری جدید» در حالتِ عادی است** (`.btn-bluish`): همان
 * زمینهٔ تهرنگِ ۱۲ و در تمِ روشن همان بوردرِ ۱۳. یعنی هاورِ منو و حالتِ آرامِ
 * دکمه‌های برنامه یک زبان دارند و کاربر دو لهجهٔ متفاوت از یک رنگ نمی‌بیند.
 *
 * ⚠️ **در تمِ تیره بوردر هم‌رنگِ زمینه است، یعنی `transparent` — نه توکنِ ۱۲.**
 * زمینه به‌طورِ پیش‌فرض زیرِ بوردر هم کشیده می‌شود (`background-clip: border-box`)
 * و بوردرِ نیمه‌شفاف **رویش** می‌نشیند؛ پس بوردرِ ۱۲ روی زمینهٔ ۱۲ لبه را دو لایه
 * تهرنگ می‌کرد (حدودِ ۹٫۷۵٪ در برابر ۵٪ِ داخل) و خطِ کم‌رنگی می‌ماند. بوردرِ
 * شفاف از همان زمینهٔ زیرش پر می‌شود، پس ردیف یکدست دیده می‌شود. پیش‌تر اینجا ۱۶
 * بود و روی کرومِ خاکستری مثلِ قاب دورِ ردیف می‌نشست.
 *
 * ⚠️ تنها تفاوت با آن دکمه، **رنگِ متن در تمِ روشن** است: دکمه `var-color-06`
 * (خاکستریِ تیره) دارد ولی ردیفِ منو ۱۹ می‌گیرد — همان توکنی که در `index.css`
 * صریحاً «متن روی تهرنگ در تم لایت» توصیف شده. با خاکستری، هاور در تمِ روشن
 * تقریباً هم‌رنگِ حالتِ عادی می‌شد و اصلاً دیده نمی‌شد.
 *
 * ⚠️ همهٔ این‌ها از خانوادهٔ اکسنت‌اند، پس با «انتخاب رنگ» خودشان عوض می‌شوند.
 * متن در تمِ تیره ۱۵ است نه ۱۹: ۱۵ روی سفید کنتراستِ کافی ندارد و ۱۹ روی زمینهٔ
 * تیره کدر می‌شود.
 *
 * ⚠️ ردیفی که این‌ها را می‌گیرد باید در حالتِ عادی `border border-transparent`
 * داشته باشد، وگرنه با ظاهر شدنِ بوردر یک پیکسل جابه‌جا می‌شود.
 */
export const MENU_ROW_ACTIVE =
    "bg-var-color-12 text-var-color-19 dark:text-var-color-15 " +
    "border-var-color-13 dark:border-transparent";

// «سطح» (زمینه و بوردر) از «متن» جدا نگه داشته شده چون کشوی انتخاب رنگ فقط
// اولی را می‌خواهد
const ROW_SURFACE_HOVER =
    "hover:bg-var-color-12 hover:border-var-color-13 dark:hover:border-transparent";

const ROW_TEXT_HOVER = "hover:text-var-color-19 dark:hover:text-var-color-15";

export const MENU_ROW_HOVER = `${ROW_SURFACE_HOVER} ${ROW_TEXT_HOVER}`;

/**
 * هاورِ آرام — مخصوصِ ردیف‌های کشوی انتخاب رنگ.
 *
 * ⚠️ **رنگِ متن عوض نمی‌شود.** آنجا هر ردیف **خودش** یک نمونه‌رنگ نشان می‌دهد و
 * تیک، نشانهٔ رنگِ فعلیِ برنامه است. اگر متن و تیک هم با هاور اکسنت می‌شدند،
 * ردیفِ زیرِ موس شبیهِ ردیفِ انتخاب‌شده می‌شد و دو نشانه با هم قاطی می‌شدند.
 *
 * 📌 **دقیقاً همان هاورِ آیتم‌های سایدبار است** (`HomeSidebar.jsx`): تهرنگِ ۳٪
 * (`۶۶`) و بی‌بوردر — نه ۵٪ و بوردرِ بقیهٔ منوها. هر دو فهرستی‌اند با یک آیکون و
 * یک نام که کاربر رویشان جابه‌جا می‌شود، پس یک‌جور واکنش نشان می‌دهند. اگر هاورِ
 * سایدبار را عوض کردید، این را هم عوض کنید.
 */
export const MENU_ROW_HOVER_QUIET = "hover:bg-var-color-66";

/**
 * ردیفِ ویرانگر (حذف) از **رنگِ** این قاعده بیرون است و قرمز می‌ماند — مثل رنگِ
 * بدهکار/بستانکار، معنا دارد نه سلیقه، پس با اکسنت عوض نمی‌شود. ولی شکلش همان
 * است: تهرنگِ کم‌رنگ + بوردر، همان جفتی که دکمهٔ خروج در فوتر دارد.
 */
export const MENU_ROW_DANGER =
    "text-var-color-28 hover:bg-var-color-26 hover:border-var-color-45 dark:hover:border-transparent";

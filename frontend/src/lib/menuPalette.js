/**
 * رنگ‌بندیِ مشترکِ ردیفِ منوها — فیلترِ مشتریان و دوره، دراپ‌داونِ فوتر، منوی
 * تنظیمات، کشوی انتخاب رنگ، کشوی مدل و منوی سه‌نقطهٔ گفتگو.
 *
 * ⚠️ **همهٔ منوها باید یک‌شکل هاور شوند.** پیش‌تر هرکدام رنگِ خودش را داشت —
 * یکی زمینهٔ پُرِ فیروزه‌ای، یکی خاکستریِ روشن، یکی سرمه‌ای — و چون در فوتر
 * کنارِ هم باز می‌شوند، ناهماهنگی‌شان مستقیم دیده می‌شد. رشته‌ها اینجا یک نسخه
 * دارند و هر منو فقط وصلشان می‌کند.
 *
 * ⚠️ **زمینه‌ای گذاشته نمی‌شود.** نشانهٔ هاور فقط متن و آیکونِ اکسنت است به‌علاوهٔ
 * بوردرِ تهرنگش؛ ردیف در هر دو تم شفاف می‌ماند.
 *
 * ⚠️ رنگ‌ها از خانوادهٔ اکسنت‌اند، پس با «انتخاب رنگ» خودشان عوض می‌شوند. متن در
 * تمِ روشن نسخهٔ تیره‌ترِ اکسنت (۱۹) است و در تمِ تیره نسخهٔ پررنگ (۱۵)؛ ۱۵ روی
 * سفید کنتراستِ کافی ندارد و ۱۹ روی زمینهٔ تیره کدر می‌شود. بوردر (۱۳) شفافیت
 * دارد، پس روی هر دو زمینه می‌نشیند و جفتِ `dark:` نمی‌خواهد.
 *
 * ⚠️ ردیفی که این‌ها را می‌گیرد باید در حالتِ عادی `border border-transparent`
 * داشته باشد، وگرنه با ظاهر شدنِ بوردر یک پیکسل جابه‌جا می‌شود.
 */
export const MENU_ROW_ACTIVE = "text-var-color-19 dark:text-var-color-15 border-var-color-13";

export const MENU_ROW_HOVER =
    "hover:text-var-color-19 dark:hover:text-var-color-15 hover:border-var-color-13";

/**
 * ردیفِ ویرانگر (حذف) از **رنگِ** این قاعده بیرون است و قرمز می‌ماند — مثل رنگِ
 * بدهکار/بستانکار، معنا دارد نه سلیقه، پس با اکسنت عوض نمی‌شود. ولی از قاعدهٔ
 * «بی‌زمینه» بیرون نیست: اینجا هم فقط متن و بوردر رنگ می‌گیرند، وگرنه یک ردیف
 * در میانِ ردیف‌های شفاف زمینه‌دار می‌شد.
 */
export const MENU_ROW_DANGER = "text-var-color-28 hover:border-var-color-45";

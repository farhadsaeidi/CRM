/**
 * رباتِ دستیار — سرِ گنبدی با دو آنتن، تنه و دو بازو.
 *
 * چرا سفارشی و نه آیکونِ آماده؟ رباتِ کتابخانه‌ها (`TbRobot`، `RiRobot2Line` و
 * مانندشان) همه جدی و فنی‌اند — شبیهِ نمادِ صنعتی، نه چیزی که دلت بخواهد با آن
 * حرف بزنی. این فرم با چند شکلِ ساده همان حسِ دوستانه را می‌دهد.
 *
 * ⚠️ **چشم‌ها سوراخ‌اند، نه دایرهٔ رنگی.** با `fill-rule="evenodd"` از دلِ سر
 * بریده می‌شوند، پس هر چیزی که پشتِ آیکون است از میانشان دیده می‌شود. اگر
 * به‌جایش دایره‌ای هم‌رنگِ پس‌زمینه می‌گذاشتیم، همان لحظه‌ای که آیکون روی سطحِ
 * دیگری بنشیند — یا تم عوض شود — به لکه تبدیل می‌شد.
 *
 * ⚠️ **فاصلهٔ سر و تنه عمدی است.** بدونِ آن دو شکل یکی دیده می‌شوند و فرمِ
 * آشنای ربات از بین می‌رود؛ در اندازهٔ کوچک همین درز است که خوانا نگهش می‌دارد.
 *
 * رنگ از `currentColor` می‌آید و اندازه از کلاس — پس دقیقاً مثل بقیهٔ
 * آیکون‌های `react-icons` رفتار می‌کند و در هر دو تم درست می‌نشیند.
 */
const AgentIcon = ({className = "", ...props}) => (
    <svg viewBox="0 0 24 24" fill="none" className={className} aria-hidden="true" {...props}>
        {/* آنتن‌ها */}
        <path d="M7.7 2.5 9.4 5.3M16.3 2.5 14.6 5.3"
              stroke="currentColor" strokeWidth="1.6" strokeLinecap="round"/>

        {/* سر، با دو چشمِ بریده‌شده از خودش */}
        <path
            fill="currentColor"
            fillRule="evenodd"
            d="M5 11.6a7 7 0 0 1 14 0z
               M9.4 7.9a1 1 0 1 0 0 2 1 1 0 0 0 0-2z
               M14.6 7.9a1 1 0 1 0 0 2 1 1 0 0 0 0-2z"
        />

        {/* تنه */}
        <rect x="5" y="12.8" width="14" height="7.8" rx="2.2" fill="currentColor"/>

        {/* بازوها */}
        <rect x="1.5" y="13" width="2.4" height="6.2" rx="1.2" fill="currentColor"/>
        <rect x="20.1" y="13" width="2.4" height="6.2" rx="1.2" fill="currentColor"/>
    </svg>
);

export default AgentIcon;

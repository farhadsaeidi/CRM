/**
 * رباتِ دستیار — همان نشانِ هشت‌بیتی.
 *
 * ⚠️ **مسیر عیناً از فایلِ SVGِ اصلی برداشته شده و دست نخورده.** نسخهٔ قبلی را
 * خودم از روی گرید بازسازی کرده بودم و «شبیه» بود نه «همان»؛ اختلافِ چند دهمِ
 * واحد در ارتفاعِ ردیف‌ها و پهنای پاها با چشم دیده می‌شد. اگر روزی باید عوض
 * شود، فایلِ اصلی را بگیرید — بازکشیدنش با دست همان تفاوت را برمی‌گرداند.
 *
 * ⚠️ `fillRule="evenodd"` حیاتی است: دو چشم زیرمسیرهای جدا هستند و با این
 * قاعده از دلِ بدنه بریده می‌شوند. بدونش پر می‌شوند و شکل کور می‌ماند.
 *
 * ⚠️ رنگ با `currentColor` می‌آید نه رنگِ ثابتِ فایلِ اصلی (`#D97757`)، چون قرار
 * شد آیکون به پالتِ پروژه بخورد. رنگ را والد تعیین می‌کند و آیکون در هر دو تم
 * درست می‌نشیند — مثلِ بقیهٔ آیکون‌های `react-icons`.
 */
const AgentIcon = ({className = "", ...props}) => (
    <svg viewBox="0 0 24 17.25" fill="currentColor" className={className}
         aria-hidden="true" {...props}>
        {/* کشیدگیِ ۱۵٪ در ارتفاع. مسیر دست نمی‌خورد و viewBox هم‌زمان بلندتر
            می‌شود، پس نسبتِ ابعاد داخلِ خودِ svg تعریف می‌ماند و هر سه اندازهٔ
            فراخوان (w-5 و w-12) بدونِ تغییر درست می‌نشینند. */}
        <g transform="translate(0,-5.75) scale(1,1.15)">
        <path
            fillRule="evenodd"
            clipRule="evenodd"
            d="M20.998 10.949H24v3.102h-3v3.028h-1.487V20H18v-2.921h-1.487V20H15v-2.921H9V20H7.488v-2.921H6V20H4.487v-2.921H3V14.05H0V10.95h3V5h17.998v5.949zM6 10.949h1.488V8.102H6v2.847zm10.51 0H18V8.102h-1.49v2.847z"
        />
        </g>
    </svg>
);

export default AgentIcon;

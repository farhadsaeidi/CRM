/**
 * سرِ رباتِ دستیار — همان فرمِ آشنای گنبد با دو آنتن.
 *
 * چرا سفارشی و نه آیکونِ آماده؟ رباتِ کتابخانه‌ها (`TbRobot`، `RiRobot2Line` و
 * مانندشان) همه جدی و فنی‌اند — شبیهِ نمادِ صنعتی، نه چیزی که دلت بخواهد با آن
 * حرف بزنی. این فرم با سه خط ساده همان حسِ دوستانه را می‌دهد.
 *
 * ⚠️ **چشم‌ها سوراخ‌اند، نه دایرهٔ رنگی.** با `fill-rule="evenodd"` از دلِ گنبد
 * بریده می‌شوند، پس هر چیزی که پشتِ آیکون است از میانشان دیده می‌شود. اگر
 * به‌جایش دایره‌ای هم‌رنگِ پس‌زمینه می‌گذاشتیم، در تمِ دیگر یا روی هر سطحِ
 * متفاوتی لکه می‌شد.
 *
 * ⚠️ رنگ از `currentColor` می‌آید و اندازه از کلاس — پس دقیقاً مثل بقیهٔ
 * آیکون‌های `react-icons` رفتار می‌کند و در هر دو تم درست می‌نشیند.
 */
const AgentIcon = ({className = "", ...props}) => (
    <svg viewBox="0 0 24 24" fill="none" className={className} aria-hidden="true" {...props}>
        {/* آنتن‌ها — بیرون‌زده و کمی مورب، همان چیزی که فرم را بامزه می‌کند */}
        <path d="M7.4 4.6 9.3 7.6M16.6 4.6 14.7 7.6"
              stroke="currentColor" strokeWidth="1.7" strokeLinecap="round"/>

        {/* گنبد، با دو چشمِ بریده‌شده از خودش */}
        <path
            fill="currentColor"
            fillRule="evenodd"
            d="M3 15.4a9 9 0 0 1 18 0z
               M8.7 10.6a1.25 1.25 0 1 0 0 2.5 1.25 1.25 0 0 0 0-2.5z
               M15.3 10.6a1.25 1.25 0 1 0 0 2.5 1.25 1.25 0 0 0 0-2.5z"
        />
    </svg>
);

export default AgentIcon;

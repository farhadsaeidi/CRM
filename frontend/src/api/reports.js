import {api, BASE_URL} from "./client";

export const reportsApi = {
    // صورتحسابِ همهٔ مشتریان — صفحه‌بندی ندارد، چون صورتحسابِ نصفه صورتحساب نیست
    statement: () => api.get("/statement/"),
};

// ⚠️ دانلود عمداً از مسیرِ `api/client.js` نمی‌رود: آنجا پاسخ را JSON پارس
// می‌کند و اینجا بدنه یک فایلِ باینری است. رفتنِ مستقیمِ مرورگر به آدرس هم
// یعنی دیالوگِ ذخیرهٔ خودِ مرورگر و نوارِ پیشرفتش را رایگان داریم.
// کوکیِ سشن هم چون هم‌دامنه است خودبه‌خود همراه می‌رود.
export const excelExportUrl = `${BASE_URL}/export/xlsx/`;
